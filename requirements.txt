# --- WhatsApp / Telegram extraction bot (fox_tor_bot.py) ---

# Telegram Bot API — telebot package
pyTelegramBotAPI>=4.14.0

# HTTP engine
requests>=2.31.0

# SOCKS5/SOCKS5H proxy support for requests (Tor routing in IP_ROTATION mode)
PySocks>=1.7.1

# Tor controller — bootstrap monitoring, ControlPort auth, NEWNYM rotation
stem>=1.8.2
# optional — only if you deploy behind a webhook
# flask>=3.0.0

# optional — C-accelerated JSON (drop-in, faster polling)
# orjson>=3.9.0

pyTelegramBotAPI>=4.14.0
requests>=2.31.0
PySocks>=1.7.1
stem>=1.8.2
